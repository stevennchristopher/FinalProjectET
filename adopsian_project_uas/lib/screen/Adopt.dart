import 'package:flutter/material.dart';

class Adopt extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('Adopt'),
      ),
      body: Center(child: Text("This is Adopt Page "),
      ),
    );
  }
}